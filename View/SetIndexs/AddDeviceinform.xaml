<Window x:Class="WarmBox_Central_Monitoring_Station.View.SetIndexs.AddDeviceinform"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WarmBox_Central_Monitoring_Station.View.SetIndexs"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Icon="/IMG/new/david.png"
        Height="350" 
        Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#3a3a3a"
        WindowStyle="None">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/DiyStyle/Diystyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- 微钝角按钮样式 -->
            <Style x:Key="DialogButtonStyle" TargetType="Button">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Height" Value="40"/>
                <Setter Property="Width" Value="120"/>
                <Setter Property="Margin" Value="10,0,0,0"/>
                <Setter Property="Background" Value="#5a5a5a"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="#7a7a7a"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    CornerRadius="3" 
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    BorderBrush="{TemplateBinding BorderBrush}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 标签样式 -->
            <Style x:Key="LabelStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="Margin" Value="0,0,10,0"/>
            </Style>

            <Style x:Key="UnderlineReadOnlyStyle" TargetType="TextBox">
                <Setter Property="Height" Value="28"/>
                <Setter Property="Width" Value="200"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                <Setter Property="BorderBrush" Value="#7a7a7a"/>
                <Setter Property="IsReadOnly" Value="True"/>
                <Setter Property="IsEnabled" Value="True"/>
                <Setter Property="Padding" Value="0,0,0,2"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Grid>
                                <Border x:Name="border" 
                                        Background="{TemplateBinding Background}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        SnapsToDevicePixels="True"/>
                                <ScrollViewer x:Name="PART_ContentHost" 
                                              Focusable="false" 
                                              HorizontalScrollBarVisibility="Hidden" 
                                              VerticalScrollBarVisibility="Hidden"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="false">
                                    <Setter TargetName="border" Property="Opacity" Value="0.56"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="true">
                                    <Setter TargetName="border" Property="BorderBrush" Value="#7a7a7a"/>
                                </Trigger>
                                <Trigger Property="IsFocused" Value="true">
                                    <Setter TargetName="border" Property="BorderBrush" Value="#7a7a7a"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 可编辑下划线文本框样式 -->
            <Style x:Key="UnderlineEditableStyle" TargetType="TextBox" BasedOn="{StaticResource UnderlineReadOnlyStyle}">
                <Setter Property="IsReadOnly" Value="False"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="CaretBrush" Value="White"/>
            </Style>

            <!-- 关闭按钮样式 -->
            <Style x:Key="CloseButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Width" Value="20"/>
                <Setter Property="Height" Value="20"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" 
                                    Background="{TemplateBinding Background}" 
                                    BorderThickness="0"
                                    Cursor="Hand">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#696b73"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#696b73"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 内容卡片样式 -->
            <Style x:Key="ContentCardStyle" TargetType="Border">
                <Setter Property="Background" Value="#4a4a4a"/>
                <Setter Property="BorderBrush" Value="#5a5a5a"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Margin" Value="20"/>
                <Setter Property="Padding" Value="30"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="30"
                      ResizeBorderThickness="0"
                      GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>
    <Grid Background="#3a3a3a">
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 自定义标题栏 -->
        <Border Grid.Row="0" Background="#3a3a3a" BorderBrush="#5a5a5a" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 图标和标题 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                    <Image Source="/IMG/new/david.png" Width="16" Height="16" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding WindowTitle}" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- 关闭按钮 -->
                <Button Grid.Column="2" 
                        Style="{StaticResource CloseButtonStyle}"
                        Command="{Binding CancelCommand}"
                        Margin="0,0,10,0"
                        WindowChrome.IsHitTestVisibleInChrome="True">
                    <Image Source="/IMG/new/close.png" Width="18" Height="18"/>
                </Button>
            </Grid>
        </Border>

        <!-- 主要内容区域 -->
        <Border Grid.Row="1" Style="{StaticResource ContentCardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 第一行：设备编号 + 品牌 -->
                <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}">
                    <Run Text="{DynamicResource DeviceNumber}"/><Run Text="："/>
                </TextBlock>
                <TextBox Grid.Row="0" Grid.Column="1"
                         Style="{StaticResource UnderlineEditableStyle}"
                         Text="{Binding DeviceNumber}"
                         IsReadOnly="{Binding IsReadOnly}"
                         HorizontalAlignment="Left"/>

                <TextBlock Grid.Row="0" Grid.Column="2" Style="{StaticResource LabelStyle}" Margin="20,0,10,0">
                    <Run Text="{DynamicResource Brand}"/><Run Text="："/>
                </TextBlock>
                <ComboBox Grid.Row="0" Grid.Column="3"
                          Style="{StaticResource StatusComboBoxStyle}"
                          ItemsSource="{Binding Brands}"
                          SelectedItem="{Binding SelectedBrand}"
                          IsEnabled="{Binding IsReadOnly, Converter={StaticResource InverseBooleanConverter}}"
                          HorizontalAlignment="Left">
                    <ComboBox.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#4a4a4a"/>
                    </ComboBox.Resources>
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Foreground="White" Width="180"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <!-- 第二行：仪器类型 + 设备型号 -->
                <TextBlock Grid.Row="1" Grid.Column="0" Style="{StaticResource LabelStyle}" Margin="0,20,10,0">
                    <Run Text="{DynamicResource InstrumentType}"/><Run Text="："/>
                </TextBlock>
                <ComboBox Grid.Row="1" Grid.Column="1"
                          Style="{StaticResource StatusComboBoxStyle}"
                          ItemsSource="{Binding DeviceTypes}"
                          SelectedItem="{Binding SelectedDeviceType}"
                          IsEnabled="True"
                          HorizontalAlignment="Left"
                          Margin="0,20,0,0">
                    <ComboBox.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#4a4a4a"/>
                    </ComboBox.Resources>
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Foreground="White" Width="180"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Grid.Row="1" Grid.Column="2" Style="{StaticResource LabelStyle}" Margin="20,20,10,0">
                    <Run Text="{DynamicResource DeviceModel}"/><Run Text="："/>
                </TextBlock>
                <ComboBox Grid.Row="1" Grid.Column="3"
                          Style="{StaticResource StatusComboBoxStyle}"
                          ItemsSource="{Binding DeviceModels}"
                          SelectedItem="{Binding SelectedDeviceModel}"
                          IsEnabled="{Binding IsReadOnly, Converter={StaticResource InverseBooleanConverter}}"
                          HorizontalAlignment="Left"
                          Margin="0,20,0,0">
                    <ComboBox.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#4a4a4a"/>
                    </ComboBox.Resources>
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Foreground="White" Width="180"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <!-- 第三行：IP地址 + 科室(隐藏) -->
                <TextBlock Grid.Row="2" Grid.Column="0" Style="{StaticResource LabelStyle}" Margin="0,20,10,0">
                    <Run Text="{DynamicResource IPAddress}"/><Run Text="："/>
                </TextBlock>
                <TextBox Grid.Row="2" Grid.Column="1"
                         Style="{StaticResource UnderlineEditableStyle}"
                         Text="{Binding IPAddress}"
                         IsReadOnly="{Binding IsReadOnly}"
                         HorizontalAlignment="Left"
                         Margin="0,20,0,0"/>

                <TextBlock Grid.Row="2" Grid.Column="2" Text="{DynamicResource Department}" Style="{StaticResource LabelStyle}" Margin="20,20,10,0" Visibility="Collapsed"/>
                <TextBox Grid.Row="2" Grid.Column="3" Visibility="Collapsed"
                         Style="{StaticResource UnderlineEditableStyle}"
                         IsReadOnly="{Binding IsReadOnly}"
                         Text="{Binding Department, Mode=OneWay}"
                         HorizontalAlignment="Left"
                         Margin="0,20,0,0"/>
            </Grid>
        </Border>

        <!-- 按钮区域 -->
        <Border Grid.Row="2" Background="#3a3a3a" Padding="15">
            <Grid HorizontalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="140"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="{DynamicResource Confirm}" 
                        Style="{StaticResource DialogButtonStyle}" 
                        Cursor="Hand"
                        Command="{Binding SaveCommand}"/>

                <Button Grid.Column="1"
                        Content="{DynamicResource Cancel}" 
                        Cursor="Hand"
                        Style="{StaticResource DialogButtonStyle}" 
                        Command="{Binding CancelCommand}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
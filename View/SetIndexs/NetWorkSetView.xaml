<UserControl x:Class="WarmBox_Central_Monitoring_Station.View.SetIndexs.NetWorkSetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WarmBox_Central_Monitoring_Station.View.SetIndexs"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/DiyStyle/Diystyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
            </Grid.RowDefinitions>
            <TextBlock Text="{DynamicResource CentralStationServer}" FontWeight="Bold" FontSize="18" Grid.Row="0" Foreground="White"  Margin="15"/>
            <Border Grid.Row="1" Padding="25,5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"></ColumnDefinition>
                        <ColumnDefinition Width="150" MinWidth="150"></ColumnDefinition>
                        <ColumnDefinition Width="auto"></ColumnDefinition>
                        <ColumnDefinition Width="150" MinWidth="150"></ColumnDefinition>
                        <ColumnDefinition Width="105"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Margin="5" VerticalAlignment="Center" Foreground="White" FontSize="15">
                        <Run Text="{DynamicResource IPAddress}"/><Run Text=":"/>
                    </TextBlock>
                    <TextBox Style="{StaticResource textboxstyles}" Grid.Column="1" VerticalAlignment="Center" Margin="5" Text="{Binding IPaddress}"/>
                    <TextBlock Grid.Column="2" Margin="50,5,5,5" VerticalAlignment="Center" Foreground="White" FontSize="15">
                        <Run Text="{DynamicResource PortNumber}"/><Run Text=":"/>
                    </TextBlock>
                    <TextBox Style="{StaticResource textboxstyles}" Grid.Column="3" VerticalAlignment="Center" Margin="5" Text="{Binding Port}"/>
                    <Button Content="{DynamicResource Edit}" Style="{StaticResource IconButtonStyle_xq3}" Background="#3c3c3c"  Margin="15,5" Grid.Column="4" HorizontalContentAlignment="Center" Command="{Binding SaveCommand}"></Button>
                </Grid>
            </Border>
        </Grid>
        <Grid Height="50" Grid.Row="1">
            <TextBlock Text="{DynamicResource RestartNote}" Margin="100,0,0,0" VerticalAlignment="Center" Foreground="#A9A9A9" TextWrapping="Wrap"></TextBlock>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
            </Grid.RowDefinitions>
            <TextBlock Text="{DynamicResource NetworkTest}" FontWeight="Bold" FontSize="18" Grid.Row="0" Foreground="White"  Margin="15"/>
            <Border Grid.Row="1" Padding="25,5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"></ColumnDefinition>
                        <ColumnDefinition Width="250" MinWidth="150"></ColumnDefinition>
                        <ColumnDefinition Width="170" MinWidth="150"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Margin="5" VerticalAlignment="Center" Foreground="White" FontSize="15">
                        <Run Text="{DynamicResource TargetAddress}"/><Run Text=":"/>
                    </TextBlock>
                    <TextBox Style="{StaticResource textboxstyles}" Grid.Column="1" VerticalAlignment="Center" Margin="5" Text="{Binding PingIpAddress}"/>
                    <Button Content="{DynamicResource TestNetworkConnection}" Style="{StaticResource IconButtonStyle_xq3}" Background="#3c3c3c"  Margin="15,2" Grid.Column="2" Command="{Binding  PingIPServiceCommand}"></Button>
                </Grid>
            </Border>
            <TextBox Grid.Row="2"
              Text="{Binding PingResult}"
              Style="{StaticResource PingResultTextBoxStyle}"
              VerticalAlignment="Stretch"
              HorizontalAlignment="Stretch"
              Height="150"
              Margin="15,10"
              Padding="10"/>
        </Grid>
    </Grid>
</UserControl>
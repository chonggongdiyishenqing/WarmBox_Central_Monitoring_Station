<UserControl x:Class="WarmBox_Central_Monitoring_Station.View.WarmBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WarmBox_Central_Monitoring_Station.View"
             xmlns:converters="clr-namespace:WarmBox_Central_Monitoring_Station.Converters"
             Background="#2C2C2C">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
            <ResourceDictionary Source="/DiyStyle/Diystyle.xaml"/>
        </ResourceDictionary.MergedDictionaries>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:BoolToInvertVisibilityConverter x:Key="BoolToInvertVisibilityConverter"/>
        <Style x:Key="TransparentButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 三个数据模板，每个模板都包含一个按钮来触发命令 -->
        <DataTemplate x:Key="Template16">
            <Button Command="{Binding DataContext.ShowDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CommandParameter="{Binding}"
                    Cursor="Hand"
                    Style="{StaticResource TransparentButtonStyle}">
                <local:BedView16 DataContext="{Binding}" />
            </Button>
        </DataTemplate>
        <DataTemplate x:Key="Template8">
            <Button Command="{Binding DataContext.ShowDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CommandParameter="{Binding}"
                    Cursor="Hand"
                    Style="{StaticResource TransparentButtonStyle}">
                <local:BedView8 DataContext="{Binding}" />
            </Button>
        </DataTemplate>
        <DataTemplate x:Key="TemplateDetailLeft">
            <Button Command="{Binding DataContext.ShowDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CommandParameter="{Binding}"
                    Cursor="Hand"
                    Style="{StaticResource TransparentButtonStyle}">
                <local:BedDetailCard DataContext="{Binding}" />
            </Button>
        </DataTemplate>

        <converters:BedCardTemplateSelector x:Key="CardSelector"
                                            Template16="{StaticResource Template16}"
                                            Template8="{StaticResource Template8}"
                                            TemplateDetailLeft="{StaticResource TemplateDetailLeft}"/>
            </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <!-- 正常模式：分页网格 -->
        <Grid Visibility="{Binding IsDetailMode, Converter={StaticResource BoolToInvertVisibilityConverter}}">
            <ItemsControl ItemsSource="{Binding DisplayBeds}" 
                          ItemTemplateSelector="{StaticResource CardSelector}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="{Binding GridColumns}" Rows="{Binding GridRows}" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </Grid>

        <!-- 详情模式：左右分屏 -->
        <Grid Visibility="{Binding IsDetailMode, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.5*"/>
                <ColumnDefinition Width="7.5*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：隐藏滚动条但可滚动，只显示实际床位 -->
            <ScrollViewer Grid.Column="0" 
                          VerticalScrollBarVisibility="Hidden"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding DetailModeBeds}" 
                              ItemTemplateSelector="{StaticResource CardSelector}"
                               Margin="1,0,0,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="2" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>

            <!-- 右侧：详情控件 -->
            <Border Grid.Column="1" Background="#3A3A3A" Margin="2">
                <local:BedDetialPopup DataContext="{Binding SelectedBedDetailVM}" />
            </Border>
        </Grid>
    </Grid>
</UserControl>